<form action="{{ path('fos_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="form-horizontal well" novalidate>
    {{ form_widget(form) }}
    <div class="form-actions">
        <a href="{{ path('fos_user_profile_show') }}" class="btn">Back to Profile</a>
        <input type="submit" class="btn btn-primary" value="{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>
